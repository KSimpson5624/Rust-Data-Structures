[package]
name = "data_structures"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = "0.8.2"
rand = { version = "0.10.2", features = ["thread_rng"] }

[[bench]]
name = "smartset"
harness = false
